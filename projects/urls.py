"""
URL routes for the projects API. Endpoints are wired in Phase 9.
"""
from django.urls import path

app_name = "projects"

urlpatterns: list = [
    # path("", ProjectViewSet.as_view({"get": "list", "post": "create"}), name="project-list"),
]
